import styled from "styled-components";

// Is not a div, more like a side element
export const Wrapper = styled.aside`
  font-family: Arial, Helvetica, sans-serif;
  width: 500px;
  padding: 20px;
`;
